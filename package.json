{
  "homepage": "https://tinat10.github.io/portfolio",

  "name": "tina-thai-portfolio",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "5.0.1",
    "react-router-dom": "^6.20.0",
    "framer-motion": "^10.16.4",
    "@emailjs/browser": "^3.11.0",
    "react-icons": "^4.11.0"
  },
  "scripts": {
    "generate-carousel": "node scripts/generateCarousel.js",
    "prestart": "npm run generate-carousel",
    "prebuild": "npm run generate-carousel",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
